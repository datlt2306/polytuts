---
id: hello-world
sidebar_position: 1
---

# Hello world

Mở đầu về Javascript

import BrowserWindow from "@site/src/components/BrowserWindow";
import CodeBlock from "@theme/CodeBlock";
import Link from "@docusaurus/Link";

<Link to="http://abc">
    <img src="https://developer.stackblitz.com/img/open_in_stackblitz.svg" />
</Link>

<BrowserWindow url="http://localhost:5173/admin/projects">

```js
import { deleteProject, getProjects } from "@/api/project";
import { useEffect, useState } from "@/lib";
import axios from "axios";

const AdminProjectsPage = () => {
    // projects  = 3
    const [projects, setProjects] = useState([]);

    useEffect(() => {
        (async () => {
            try {
                setProjects(await getProjects());
            } catch (error) {
                console.log(error);
            }
        })();
    }, []);
    useEffect(() => {
        const btns = document.querySelectorAll(".btn-remove");
        for (let btn of btns) {
            btn.addEventListener("click", function () {
                const id = this.dataset.id;
                // xóa trên server
                deleteProject(id).then(() => {
                    const newsProject = projects.filter((project) => project.id != id);
                    setProjects(newsProject);
                });
            });
        }
    });

    return `<div class="container mt-5">
                <h1>Quản lý dự án</h1>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>STT</th>
                            <th>Tên dự án</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        ${projects
                            .map((project, index) => {
                                return `
                                <tr>
                                    <td>${index + 1}</td>
                                    <td>${project.name}</td>
                                    <td width="150">
                                        <button data-id="${
                                            project.id
                                        }" class="btn btn-danger btn-remove">Xóa</button>
                                    </td>
                                </tr>
                            `;
                            })
                            .join("")}
                    </tbody>
                </table>

    </div>`;
};

export default AdminProjectsPage;
```

```jsx
<ShowIno name="Dat" />
```

<button onClick={() => alert("Hello, world!")}>Click me!</button>
</BrowserWindow>

## Kiểu dữ liệu

Giới thiệu kiểu dữ liệu

## Biến

[![Edit cranky-hodgkin-nxinoc](https://codesandbox.io/static/img/play-codesandbox.svg)](https://codesandbox.io/s/cranky-hodgkin-nxinoc?fontsize=14&theme=dark)

{" "}

<iframe
    src="https://codesandbox.io/embed/cranky-hodgkin-nxinoc?autoresize=1&codemirror=1&eslint=1&fontsize=14&hidenavigation=1&theme=dark&view=preview"
    style={{ width: "100%", height: "500px", border: 0, borderRadius: "4px", overflow: "hidden" }}
    title="cranky-hodgkin-nxinoc"
    allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
    sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
></iframe>
